# Local secrets and env (never commit)
.env

# Local Playwright storage state (cookies / localStorage); never commit.
.auth/

shopping.txt

node_modules/
.auth/
playwright-report/
test-results/
blob-report/
playwright/.cache/
